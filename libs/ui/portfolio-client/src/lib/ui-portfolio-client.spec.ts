import { uiPortfolioClient } from './ui-portfolio-client';

describe('uiPortfolioClient', () => {
  it('should work', () => {
    expect(uiPortfolioClient()).toEqual('ui-portfolio-client');
  });
});
